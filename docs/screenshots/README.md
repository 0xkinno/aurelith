# Required product captures

These files must be captured from the real running AURELITH interface. They must not be composited or fabricated:

- `aurelith-hero.png` — `/` at 1440px after assets load.
- `aurelith-policy.png` — `/app/policies/new` with the wallet disconnected and no transaction implied.
- `aurelith-proof.png` — `/proof` showing the current honest FDC-blocked/FCC-ready evidence state.
- `aurelith-security.png` — `/security` showing the public/private boundary.

`aurelith-banner.png` is a real supplied AURELITH visual asset, not a product screenshot.
