import { Eyebrow } from "@/components/Eyebrow";
import { Reveal } from "@/components/Reveal";

const publicData = ["Policy commitment","Recipient bindings","Proof digest","Result digest","Nonce + timestamps","Settlement transaction"];
const privateData = ["Commercial reference","Raw revenue records","Private deductions","Margin logic","Customer context","Confidential inputs"];
const controls = [["01","POLICY BINDING","Every result belongs to one versioned policy and chain."],["02","ACTIVE TEE","Only a signer active for extension 66235 can authenticate a result."],["03","REPLAY DEFENCE","Proof digests, result digests and nonces are consumed once."],["04","FAIL CLOSED","Infrastructure uncertainty cannot become a payable business outcome."]];

export default function Security() { return <main className="editorial-page security-paper"><section className="page-hero flagship-section"><Eyebrow>SECURITY / BOUNDARIES</Eyebrow><h1>Make the minimum<br /><em>public.</em></h1><p className="lede">The chain receives enough information to enforce a result—not enough to reconstruct the private business record.</p></section><section className="privacy-boundary flagship-section"><Reveal><article className="boundary-card public-card"><div className="boundary-label"><span>PUBLIC</span><i>ON-CHAIN</i></div><h2>What settlement must reveal.</h2><ul>{publicData.map((item,index)=><li key={item}><span>0{index+1}</span>{item}</li>)}</ul></article></Reveal><div className="boundary-axis"><span>VERIFIABLE</span><i /><b>CRYPTographic boundary</b><i /><span>CONFIDENTIAL</span></div><Reveal delay={100}><article className="boundary-card private-card"><div className="boundary-label"><span>PRIVATE</span><i>SEALED</i></div><h2>What the protocol protects.</h2><ul>{privateData.map((item,index)=><li key={item}><span>0{index+1}</span>{item}</li>)}</ul></article></Reveal></section><section className="security-controls flagship-section"><div><Eyebrow>CONTRACT-LAYER CONTROLS</Eyebrow><h2>Security is a state machine,<br />not a promise.</h2></div><div className="control-grid">{controls.map(([n,t,d],index)=><Reveal key={n} delay={index*60}><article><span>{n}</span><h3>{t}</h3><p>{d}</p></article></Reveal>)}</div></section><section className="risk-notice flagship-section"><span>TESTNET</span><p>AURELITH is a Private multi-party revenue settlement: prove the payment, keep the economics private, settle the result.</p></section></main>; }
